const { createMuiTheme } = require("@material-ui/core");

const theme = createMuiTheme({
    palette: {
      primary: {
        main: "#ff5722",
        white: "#fff",
      },
    },
  });

  export default theme;